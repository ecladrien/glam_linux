# Copier ce fichier en deploy/ubuntu/glam.env puis adapter les valeurs.

# Secret RTSP (lu par src/config/manager.py)
RTSP_PASSWORD=

# Optionnel: forcer un backend Qt (par défaut xcb)
# QT_QPA_PLATFORM=xcb
